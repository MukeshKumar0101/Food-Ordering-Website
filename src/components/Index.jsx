

import { useEffect, useState } from "react"

export default function Index(){
 const [count , setClount] = useState(0)
 const inputRef = useref(null)

 useEffect(()=>{
    if(inputRef.current){}
 },[])

  return (
    <div>
      
    </div>
  )
  
}